#include <rawast/node.hpp>

namespace rawast {
// All Node operations are header-only at present. This TU exists so the
// library has an object file for the node module to which later builder
// helpers can be attached without touching headers.
} // namespace rawast
